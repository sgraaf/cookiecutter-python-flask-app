Hi {{ user.username }},

A request has been received to reset the password for your {{ project.title }} account. To reset your password, paste the following link in your browser's address bar:

{{ url_for('auth.reset_password', token=token, _external=True) }}

If you have not requested a password reset, you can simply ignore this message.

Regards,
{{ project.title }}
